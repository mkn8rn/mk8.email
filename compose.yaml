name: mk8-email-control-plane

services:
  database:
    image: postgres:18.3-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: mk8email
      POSTGRES_USER: mk8email
      POSTGRES_PASSWORD_FILE: /run/secrets/database_password
    secrets:
      - database_password
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mk8email -d mk8email"]
      interval: 10s
      timeout: 5s
      retries: 10
    security_opt:
      - no-new-privileges:true
    networks:
      - backend
    logging:
      options:
        max-size: 10m
        max-file: "3"

  admin:
    image: mk8.email-admin:local
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    init: true
    depends_on:
      database:
        condition: service_healthy
    environment:
      ASPNETCORE_URLS: http://0.0.0.0:8080
      MK8EMAIL_CONFIG_FILE: /run/secrets/mk8email_config
    secrets:
      - mk8email_config
      - database_password
      - tls_certificate
      - tls_private_key
      - dkim_private_key
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - admin-state:/var/lib/mk8email-admin
      - admin-logs:/var/log/mk8email-admin
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    ulimits:
      nofile:
        soft: 4096
        hard: 8192
    stop_grace_period: 30s
    networks:
      - backend
      - edge
    logging:
      options:
        max-size: 10m
        max-file: "3"

secrets:
  mk8email_config:
    file: ./deploy/mk8email.config.json
  database_password:
    file: ./deploy/secrets/database_password
  tls_certificate:
    file: ./deploy/secrets/tls_certificate.pem
  tls_private_key:
    file: ./deploy/secrets/tls_private_key.pem
  dkim_private_key:
    file: ./deploy/secrets/dkim_private_key.pem

volumes:
  postgres-data:
  admin-state:
  admin-logs:

networks:
  backend:
    internal: true
  edge:
    ipam:
      config:
        - subnet: 172.30.8.0/24
